{
  "name": "orccb",
  "version": "0.1.0",
  "private": true,
  "description": "Free, open Oregon CCB pre-license course site",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-content.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
